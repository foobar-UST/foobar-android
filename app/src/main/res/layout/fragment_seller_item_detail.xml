<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="viewModel"
            type="com.foobarust.android.sellerdetail.SellerItemDetailViewModel" />
        <import type="com.foobarust.domain.models.seller.SellerItemDetailKt" />
        <import type="com.foobarust.android.utils.BindingConverters" />
        <import type="com.foobarust.android.R"/>
    </data>

    <RelativeLayout
        android:id="@+id/bottom_sheet_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <RelativeLayout
            android:id="@+id/header_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="@dimen/spacing_large"
            android:background="?attr/colorPrimarySurface">

            <TextView
                android:id="@+id/title_text_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginEnd="@dimen/spacing_large"
                android:layout_toStartOf="@id/price_text_view"
                android:text="@{viewModel.itemDetail != null ? SellerItemDetailKt.getNormalizedTitle(viewModel.itemDetail) : ``}"
                android:textAppearance="?attr/textAppearanceSubtitle1"
                android:textColor="?attr/colorOnPrimary"
                android:ellipsize="end"
                android:lineSpacingMultiplier="1.2"
                tools:text="Item Title" />

            <TextView
                android:id="@+id/price_text_view"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:text="@{@string/seller_item_data_format_price(viewModel.itemDetail.price)}"
                android:textStyle="bold"
                android:textAppearance="?attr/textAppearanceSubtitle1"
                android:textColor="?attr/colorSecondary"
                android:lineSpacingMultiplier="1.2"
                tools:text="$ 57.4"/>

            <TextView
                android:id="@+id/description_text_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/spacing_xmedium"
                android:layout_marginEnd="@dimen/spacing_large"
                android:layout_below="@id/title_text_view"
                android:layout_toStartOf="@id/price_text_view"
                android:text="@{viewModel.itemDetail != null ? SellerItemDetailKt.getNormalizedDescription(viewModel.itemDetail) : ``}"
                android:textAppearance="?attr/textAppearanceBody2"
                android:textColor="@color/material_on_primary_emphasis_medium"
                android:ellipsize="end"
                android:lineSpacingMultiplier="1.2"
                app:goneIf="@{viewModel.itemDetail.description == null}"
                tools:text="Item description will be shown here" />

            <com.google.android.material.imageview.ShapeableImageView
                android:id="@+id/photo_image_view"
                android:layout_width="match_parent"
                android:layout_height="120dp"
                android:layout_marginTop="@dimen/spacing_large"
                android:layout_below="@id/description_text_view"
                android:scaleType="centerCrop"
                app:glideUrl="@{viewModel.itemDetail.imageUrl}"
                app:glideCenterCrop="@{true}"
                app:glidePlaceholder="@{R.drawable.placeholder_card}"
                app:shapeAppearance="@style/ShapeAppearance.Foobar.SmallComponent"
                app:goneIf="@{viewModel.itemDetail.imageUrl == null}"
                tools:src="@sample/sample_image_1"/>

        </RelativeLayout>

        <TextView
            android:id="@+id/amount_title_text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_large"
            android:layout_marginBottom="@dimen/spacing_xmedium"
            android:layout_marginStart="@dimen/spacing_large"
            android:layout_below="@id/header_layout"
            android:text="@string/seller_item_subtitle_amount"
            android:textAppearance="?attr/textAppearanceSubtitle1" />

        <TextView
            android:id="@+id/item_count_text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="@dimen/spacing_large"
            android:layout_alignTop="@id/amount_title_text_view"
            android:layout_alignBottom="@id/amount_title_text_view"
            android:layout_alignParentEnd="true"
            android:gravity="center_vertical"
            android:text="@{@string/seller_item_detail_format_item_count(viewModel.itemDetail.count)}"
            android:textAppearance="?attr/textAppearanceBody2"
            android:textColor="@color/material_on_surface_emphasis_medium"/>

        <LinearLayout
            android:id="@+id/amount_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/spacing_large"
            android:layout_marginEnd="@dimen/spacing_large"
            android:layout_below="@id/amount_title_text_view"
            android:padding="@dimen/spacing_medium"
            android:orientation="horizontal"
            android:gravity="center">

            <ImageButton
                android:id="@+id/amount_increment_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:padding="@dimen/min_icon_touch_padding"
                android:src="@drawable/ic_add"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/seller_item_detail_increment_content_desc"
                app:tint="@color/material_on_surface_emphasis_medium"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:paddingStart="@dimen/spacing_mxlarge"
                android:paddingEnd="@dimen/spacing_mxlarge"
                android:gravity="center"
                android:text="@{BindingConverters.intToString(viewModel.amountsInput)}"
                android:textColor="?attr/colorPrimary"
                android:textAppearance="?attr/textAppearanceHeadline6"
                tools:text="1"/>

            <ImageButton
                android:id="@+id/amount_decrement_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:padding="@dimen/min_icon_touch_padding"
                android:src="@drawable/ic_remove"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/seller_item_detail_decrement_content_desc"
                app:tint="@color/material_on_surface_emphasis_medium"/>

        </LinearLayout>

        <FrameLayout
            android:id="@+id/submit_to_cart_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/amount_layout">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/submit_to_cart_button"
                android:layout_width="match_parent"
                android:layout_height="52dp"
                android:layout_margin="@dimen/spacing_large"
                android:text="@{viewModel.cartItemSubmitting == false ? @string/seller_item_add_to_cart(viewModel.finalPrice) : ``}"
                app:iconGravity="textStart"
                app:iconPadding="@dimen/spacing_large"
                tools:text="Add to Cart"/>

            <ProgressBar
                android:id="@+id/submit_to_cart_progress_bar"
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:layout_gravity="center"
                android:indeterminateTint="?attr/colorOnPrimary"
                android:elevation="3dp"
                app:goneIf="@{viewModel.cartItemSubmitting == false}" />

        </FrameLayout>

    </RelativeLayout>

</layout>