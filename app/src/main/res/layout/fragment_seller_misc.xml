<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="viewModel"
            type="com.foobarust.android.sellermisc.SellerMiscViewModel" />
        <import type="com.foobarust.android.states.UiFetchState" />
    </data>

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="?android:attr/colorBackground"
        app:layoutFullscreen="@{true}">

        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/map_view"
            android:layout_width="match_parent"
            android:layout_height="270dp"
            android:name="com.google.android.gms.maps.SupportMapFragment"
            tools:background="@sample/sample_image_1"/>

        <com.google.android.material.appbar.MaterialToolbar
            style="@style/Widget.MaterialComponents.Toolbar.Surface"
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            app:navigationIcon="@drawable/ic_arrow_back"
            app:paddingTopSystemWindowInsets="@{true}"/>

        <LinearLayout
            android:id="@+id/bottom_sheet_layout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            app:layout_behavior="com.google.android.material.bottomsheet.BottomSheetBehavior"
            app:marginTopSystemWindowInsets="@{true}"
            app:bottomSheetBackground="@{true}"
            tools:background="?attr/colorSurface">

            <include layout="@layout/bottom_sheet_pill"/>

            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent">

                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/recycler_view"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
                    app:goneIf="@{!(viewModel.uiFetchState instanceof UiFetchState.Success)}"
                    tools:listitem="@layout/seller_misc_address_item"
                    tools:itemCount="1"/>

                <ProgressBar
                    android:id="@+id/progress_bar"
                    android:layout_width="28dp"
                    android:layout_height="28dp"
                    android:layout_marginTop="@dimen/spacing_xlarge"
                    android:layout_gravity="center_horizontal"
                    app:goneIf="@{!(viewModel.uiFetchState instanceof UiFetchState.Loading)}" />

                <include
                    layout="@layout/load_error_layout"
                    android:id="@+id/load_error_layout"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/spacing_xlarge"
                    app:goneIf="@{!(viewModel.uiFetchState instanceof UiFetchState.Error)}" />

            </FrameLayout>

        </LinearLayout>

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

</layout>
